services:
  - type: web
    name: tail-stock-screener
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py --serve
    envVars:
      - key: PORT
        value: "10000"
      - key: SCAN_LIMIT
        value: "99999"
